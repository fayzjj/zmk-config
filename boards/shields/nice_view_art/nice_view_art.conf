# Disable the standard nice!view status widget source so this shield can
# provide zmk_display_status_screen() without a duplicate symbol.
CONFIG_NICE_VIEW_WIDGET_STATUS=n

# Keep the display in custom-status-screen mode.
CONFIG_ZMK_DISPLAY_STATUS_SCREEN_CUSTOM=y
CONFIG_LV_USE_IMG=y
